// using cursor manually:

List<String> items = new ArrayList<String>(cursor.getCount());
if (cursor.moveToFirst()) {
	do {
		items.add(ShopperOpenHelper.getItemName(cursor));
		cursor.moveToNext();
	} while (!cursor.isAfterLast());
}
cursor.close();
